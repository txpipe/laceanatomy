import { TopicMeta } from "~/components";

const TOPICS: Record<string, TopicMeta> = {
  tx_inputs: {
    title: "Transaction Inputs",
    description: (
      <>
        <p>These are the inputs of the</p>
      </>
    ),
  },
};

export default TOPICS;
